<Window x:Class="ORM_MVVM_WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ORM_MVVM_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ContentControl  Content="{Binding SelectedViewModel}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Login" Margin ="10" Width="200" Command="{Binding UpdateViewCommand}" CommandParameter="login" />
            <Button Content="SignUp" Margin ="10" Width="200"  Command="{Binding UpdateViewCommand}" CommandParameter="signup" />
        </StackPanel>
    </Grid>

</Window>
